import { __exports as createFilter$1 } from '../../../../_virtual/create-filter.js_commonjs-exports';
export { __exports as default } from '../../../../_virtual/create-filter.js_commonjs-exports';
import './normalize.js';
import './util.js';
import { __exports as normalize } from '../../../../_virtual/normalize.js_commonjs-exports';
import { __exports as util } from '../../../../_virtual/util.js_commonjs-exports';

Object.defineProperty(createFilter$1, "__esModule", { value: true });
createFilter$1.createFilter = void 0;
const normalize_1 = normalize;
const util_1 = util;
function createFilter(options, ...args) {
    let criteria = args.length <= 1 ? args[0] : args;
    let filters = normalize_1.normalize(criteria, options);
    pathFilter[util_1._filters] = filters;
    return pathFilter;
    function pathFilter(...args) {
        // Does the file path match any of the exclude filters?
        let exclude = filters.exclude.some((filter) => filter(...args));
        if (exclude) {
            return false;
        }
        if (filters.include.length === 0) {
            // Include everything that's not excluded
            return true;
        }
        // Does the file path match any of the include filters?
        let include = filters.include.some((filter) => filter(...args));
        return include;
    }
}
createFilter$1.createFilter = createFilter;
//# sourceMappingURL=create-filter.js.map
