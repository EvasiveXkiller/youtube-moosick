import { __exports as pending$1 } from '../../../../../_virtual/pending.js_commonjs-exports';
export { __exports as default } from '../../../../../_virtual/pending.js_commonjs-exports';

Object.defineProperty(pending$1, "__esModule", { value: true });
pending$1.pending = void 0;
/**
 * Returns a `Promise` and the functions to resolve or reject it.
 * @internal
 */
function pending() {
    let resolve, reject;
    let promise = new Promise((res, rej) => {
        resolve = res;
        reject = rej;
    });
    return {
        promise,
        resolve(result) {
            // eslint-disable-next-line @typescript-eslint/no-floating-promises
            Promise.resolve(result).then(resolve);
        },
        reject(reason) {
            Promise.reject(reason).catch(reject);
        }
    };
}
pending$1.pending = pending;
//# sourceMappingURL=pending.js.map
